<div class="container notification-index">
  <h3 class="mt-3 text-center">通知</h3>
  <div class="mt-3 no-gutters row list-group list-group-flush">

    <% if @notifications.exists? %>
          <%= render @notifications %>
          <%= paginate @notifications %>
    <% else %>

      <p>通知はありません</p>

    <% end %>

  </div>
</div> 
