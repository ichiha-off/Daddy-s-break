<header class="header">
  <div class="nav navbar">
    <div class="container-lg">
      <nav class='nav'>
        <%= link_to admins_categories_path, class: 'navbar-link d-lg-block d-none' do %>
          <div class="header-logo nav-item">
            <%= image_tag asset_path("Daddy_s_logo_white.svg"), class: "Daddy-s-logo" %>
            Daddy's break
          </div>
        <% end %>
        <%= link_to admins_categories_path, class: 'navbar-link d-lg-none d-block' do %>
          <div class="header-logo nav-item">
            <%= image_tag asset_path("Daddy_s_logo_white.svg"), size: "50x75", class: "pt-2" %>
          </div>
        <% end %>
      </nav>

      <nav>
        <ul class="nav justify-content-end">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">
              <div class="header-admin-text">
                Admin
              </div>
            </a>
              <div class="dropdown-menu dropdown-menu-right">

                <%= link_to admins_users_path, class: 'dropdown-item' do %>
                  <i class="fas fa-users"></i> アカウント一覧</i>
                <% end %>

                <%= link_to admins_categories_path, class: 'dropdown-item' do %>
                  <i class="fas fa-tags"></i> カテゴリ一覧</i>
                <% end %>

                <%= link_to destroy_admin_session_path, method: :delete, class: 'dropdown-item' do %>
                  <i class="fas fa-sign-out-alt"> ログアウト</i>
                <% end %>
              
              </div>
          </li>
        </ul>
      </nav>

    </div>
  </div>
</header>